#ifndef STENCIL_H
#define STENCIL_H

#define abs(a)((a) > 0 ? (a) : -(a))
#define min(a, b)((a > b) ? (b) : (a))

#ifndef SIZEX
#define SIZEX 100
#endif

#ifndef SIZEY
#define SIZEY 100
#endif

#ifndef SIZEZ
#define SIZEZ 100
#endif

#endif
